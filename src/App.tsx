import React from 'react';
import { Button } from './components/Button/Button';

function App() {
  return (
    <div className='App'>
      <Button label={'button'} size={'medium'} primary={false} />
    </div>
  );
}

export default App;
